
import { InventoryItem } from "@/utils/types";

// Mock inventory data - this will be empty since we're clearing inventory
export const mockInventoryItems: InventoryItem[] = [];

export const barNames: Record<string, string> = {
  "1": "Main Bar",
  "2": "Economa",
  "3": "Restaurant",
};
